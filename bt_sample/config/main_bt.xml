<?xml version="1.0" encoding="UTF-8"?>
<root BTCPP_format="4">
    <BehaviorTree ID="Untitled">
        <Sequence>
            <SimplePubCondition message="hello"/>
            <SimplePubCondition message="hello"/>
            <PositiveIntPubCondition message="hello"/>
        </Sequence>
    </BehaviorTree>

    <!-- Description of Node Models (used by Groot) -->
    <TreeNodesModel>
        <Action ID="SimplePubCondition" editable="true">
            <input_port name="message"/>
        </Action>
        <Action ID="PositiveIntPubCondition" editable="true">
            <input_port name="message"/>
        </Action>
    </TreeNodesModel>

</root>
